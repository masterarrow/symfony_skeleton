# Symfony API and Vue.js Skeleton

## Usage

Rename `env.example` to `.env` and fill in required data.

To run project in terminal execute:
```
./run -b
```

Available options:
```
  -b -Build images and start containers
  -s - Stop containers
  -c - Stop and remove containers
  -cm - Create migrations
  -m - Run migrations
```
